HIGH PRIORITY: rewrite entire codebase and call it "Phast" (PHP Hypertext Application Server Technology)

phpX parser 
* Templating (Sections/SectionPlaceholders) seems to work, will need further testing
* NEED to implement support for child element processing
	* .NET does this with attributes applied to the property in question... PHP doesn't support attribute metadata.